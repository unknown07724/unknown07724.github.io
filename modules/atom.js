// by unknown07724
// math module to use for projects
function rad_to_degr(radians)
{
  // Multiply radians by 180 divided by Pi to convert to degrees.
  return radians * (180/Math.PI);
}
function half(number)
{
  // Multiply radians by 180 divided by pi to convert to degrees.
  return number / 2
}
